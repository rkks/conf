# THIS IS AN AUTOGENERATED FILE, TOUCHED BY DOZENS OF SOFTWARE ON UBUNTU
# resolvconf/systemd-resolve --status/dhclient/NetworkManager to name a few.
# To avoid this file keep getting overwritten, do these:
# - Disable write/modification of the file altogether, even by sudo.
# $ sudo chattr +i /etc/resolv.conf
# - Add a new dhclient hookup script to disable dns update
# $ cat /etc/dhcp/dhclient-enter-hooks.d/nodnsupdate 
# #!/bin/sh
# make_resolv_conf(){
#    :
# }
# $ sudo chmod +x /etc/dhcp/dhclient-enter-hooks.d/nodnsupdate
# - Disable dns update in Network manager
# $ cat /etc/NetworkManager/NetworkManager.conf
# [main]
# dns=none
# Refer: https://www.cyberciti.biz/faq/dhclient-etcresolvconf-hooks/

# resolv.conf can read max 3 name-servers in /etc/resolv.conf. Also, it does
# not move to secondary name-server, if first DNS server returns lookup fail.
#nameserver 10.48.0.99
#nameserver 8.8.8.8
#nameserver 10.208.0.3
# Instead, to use different DNS for office and home, a local cached DNS server
# dnsmasq is needed. It can query relevant DNS, based on domain, reachability.
nameserver 127.0.0.1
search versa-networks.com localdomain
